type Statistics {
  hitting: OffensiveStats
}

extend type PlayerSeasonConnection {
  statistics: Statistics
}

extend type PlayerSeasonEdge {
  statistics: Statistics
}

extend type TeamSeasonEdge {
  statistics: Statistics
}
